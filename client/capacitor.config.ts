import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vr33ni.eisbachtracker',
  appName: 'Eisbach Tracker',
  webDir: 'dist'
};

export default config;
